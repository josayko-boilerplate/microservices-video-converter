# Microservices

## kubectl

Get all pods
```shell
$ kubectl get po -A
```

Apply manifests files 
```shell
$ kubectl apply -f ./
```

Apply manifests files 
```shell
$ kubectl apply -f ./
```

Restart deployment
```shell
$ kubectl rollout restart deployment <deployment_name>
```
